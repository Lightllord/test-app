import { DefRightBlockDirective } from './def-right-block.directive';

describe('DefRightBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new DefRightBlockDirective();
    expect(directive).toBeTruthy();
  });
});
