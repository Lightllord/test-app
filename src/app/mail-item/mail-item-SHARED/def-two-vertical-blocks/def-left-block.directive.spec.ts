import { DefLeftBlockDirective } from './def-left-block.directive';

describe('DefLeftBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new DefLeftBlockDirective();
    expect(directive).toBeTruthy();
  });
});
